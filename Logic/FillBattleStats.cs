using BattleOfChampions.Models;
using System.Numerics;

namespace BattleOfChampions.Logic
{
    public class FillBattleStats
    { 
        /*
        public PlayerBattleStats assignPlayerStats(Champion player)
        {
            PlayerBattleStats playerStats = new PlayerBattleStats
            {
                AdjustedAttack = player.Attack + player.Equipment.AttackModifier,
                AdjustedDefense = player.Defense + player.Equipment.DefenseModifier,
                AdjustedSpeed = player.Speed + player.Equipment.SpeedModifier,
                AdjustedHealth = player.Health + player.Equipment.HealthModifier
            };

            return playerStats;
        }

      

        public OpponentBattleStats assignOpponentStats(Champion opponent)
        {
            OpponentBattleStats opponentStats = new OpponentBattleStats();
            opponentStats.adjustedAttack = opponent.Attack + opponent.Equipment.AttackModifier;
            opponentStats.adjustedDefense = opponent.Defense + opponent.Equipment.DefenseModifier;
            opponentStats.adjustedSpeed = opponent.Speed + opponent.Equipment.SpeedModifier;
            opponentStats.adjustedHealth = opponent.Health + opponent.Equipment.HealthModifier;

            return opponentStats;
        }
        */
    }
}
